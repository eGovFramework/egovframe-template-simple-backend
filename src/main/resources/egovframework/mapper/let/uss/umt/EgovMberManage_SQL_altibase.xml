<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mberManageDAO">
	<resultMap id="stplatMap" type="egovframework.let.uss.umt.service.StplatVO">
        <result property="useStplatId"         column="USE_STPLAT_ID" />
        <result property="useStplatCn"         column="USE_STPLAT_CN" />
        <result property="infoProvdAgeCn"      column="INFO_PROVD_AGRE_CN" />
    </resultMap>
	<!-- Hsql과 호환때문에 아래 resultMap 추가: KIK -->
	<resultMap id="userList" type="egovframework.let.uss.umt.service.MberManageVO">
		<result property="uniqId" column="UNIQ_ID" />
		<result property="orgnztId" column="ORGNZT_ID" />
		<result property="userTy" column="USER_TY" />
		<result property="mberId" column="USER_ID" />
		<result property="mberNm" column="USER_NM" />
		<result property="mberEmailAdres" column="EMAIL_ADRES" />
		<result property="areaNo" column="AREA_NO" />
		<result property="middleTelno" column="MIDDLE_TEL_NO" />
		<result property="endTelno" column="END_TEL_NO" />
		<result property="moblphonNo" column="MOBL_PHON_NO" />		
		<result property="groupId" column="GROUP_ID" />
		<result property="mberSttus" column="STTUS" />
		<result property="sbscrbDe" column="SBSCR_DE" />
	</resultMap>
	
	<select id="selectMberList" parameterType="userSearchVO" resultMap="userList">
		SELECT 
		       UNIQ_ID
		     , ORGNZT_ID
		     , USER_TY
		     , USER_ID
		     , USER_NM
		     , EMAIL_ADRES
		     , AREA_NO
		     , MIDDLE_TEL_NO
		     , END_TEL_NO
		     , MIDDLE_TEL_NO
		     , GROUP_ID
		     , STTUS
		     , SBSCR_DE
		  FROM (  SELECT rownum rn, TB.* FROM (
		    	SELECT 
			           ESNTL_ID				AS UNIQ_ID
			         , ORGNZT_ID			AS ORGNZT_ID
			         , 'USR01'				AS USER_TY
			         , EMPLYR_ID			AS USER_ID
			         , USER_NM				AS USER_NM
			         , EMAIL_ADRES			AS EMAIL_ADRES
			         , AREA_NO				AS AREA_NO
			         , HOUSE_MIDDLE_TELNO	AS MIDDLE_TEL_NO
			         , HOUSE_END_TELNO		AS END_TEL_NO
			         , MBTLNUM				AS MOBL_PHON_NO
			         , GROUP_ID				AS GROUP_ID
			         , EMPLYR_STTUS_CODE	AS STTUS
			         , TO_CHAR(SBSCRB_DE, 'YYYY-MM-DD HH24:MI:SS') AS SBSCR_DE
		    	  FROM LETTNEMPLYRINFO
			     WHERE 1=1
			    <if test="sbscrbSttus != 0">AND
			    	EMPLYR_STTUS_CODE LIKE CONCAT('%' , #{sbscrbSttus}, '%')
			    </if>
			    <if test="searchCondition == 0">AND
			    	EMPLYR_ID LIKE CONCAT('%' , #{searchKeyword}, '%')
			    </if>
			    <if test="searchCondition == 1">AND
			    	USER_NM LIKE CONCAT('%' , #{searchKeyword}, '%')
			    </if>
			    ORDER BY SBSCRB_DE DESC
	    		) TB 
	    	) Z
	     WHERE rn BETWEEN #{firstIndex} + 1 AND #{firstIndex} + #{recordCountPerPage}
   	</select>
    	
   	<select id="selectMberListTotCnt" parameterType="userSearchVO" resultType="int">
        SELECT COUNT(1) AS TOTCNT
          FROM (
		        SELECT 
			           ESNTL_ID				AS UNIQ_ID
			         , ORGNZT_ID			AS ORGNZT_ID
			         , 'USR01'				AS USER_TY
			         , EMPLYR_ID			AS USER_ID
			         , USER_NM				AS USER_NM
			         , EMAIL_ADRES			AS EMAIL_ADRES
			         , AREA_NO				AS AREA_NO
			         , HOUSE_MIDDLE_TELNO	AS MIDDLE_TEL_NO
			         , HOUSE_END_TELNO		AS END_TEL_NO
			         , MBTLNUM				AS MOBL_PHON_NO
			         , GROUP_ID				AS GROUP_ID
			         , EMPLYR_STTUS_CODE	AS STTUS
			         , SBSCRB_DE			AS SBSCR_DE
	        	  FROM LETTNEMPLYRINFO
	    		) A
         WHERE 1=1
        <if test="sbscrbSttus != 0">AND
		    EMPLYR_STTUS_CODE LIKE CONCAT('%' , #{sbscrbSttus}, '%')
	    </if>
	    <if test="searchCondition == 0">AND
	    	EMPLYR_ID LIKE CONCAT('%' , #{searchKeyword}, '%')
	    </if>
	    <if test="searchCondition == 1">AND
	    	USER_NM LIKE CONCAT('%' , #{searchKeyword}, '%')
	    </if>
    </select>
    
    <insert id="insertMber_S">
        INSERT INTO LETTNEMPLYRINFO 
            (   
                  ESNTL_ID
                , ORGNZT_ID
                , EMPLYR_ID
                , USER_NM
                , PASSWORD
                , PASSWORD_HINT
                , PASSWORD_CNSR
                , IHIDNUM
                , SEXDSTN_CODE
                , ZIP
                , HOUSE_ADRES
                , AREA_NO
                , EMPLYR_STTUS_CODE
                , DETAIL_ADRES
                , HOUSE_END_TELNO
                , MBTLNUM
                , GROUP_ID
                , FXNUM
                , EMAIL_ADRES
                , HOUSE_MIDDLE_TELNO
                , SBSCRB_DE
        ) VALUES (
                  #{uniqId}
                , 'ORGNZT_0000000000000'
                , #{mberId}
                , #{mberNm}
                , #{password}
                , #{passwordHint}
                , #{passwordCnsr}
                , #{ihidnum}
                , #{sexdstnCode}
                , #{zip}
                , #{adres}
                , #{areaNo}
                , #{mberSttus}
                , #{detailAdres}
                , #{endTelno}
                , #{moblphonNo}
                , #{groupId}
                , #{mberFxnum}
                , #{mberEmailAdres}
                , #{middleTelno}
                , sysdate
        )         
    </insert>
    
    <delete id="deleteMber_S">
            DELETE FROM LETTNEMPLYRINFO 
             WHERE ESNTL_ID = #{delId}
    </delete>
    
    <select id="selectMber_S" resultType="mberVO">
        SELECT
               ESNTL_ID				AS UNIQ_ID
             , ORGNZT_ID			AS ORGNZT_ID
             , 'USR01'				AS USER_TY
             , EMPLYR_ID			AS MBER_ID
             , USER_NM				AS MBER_NM
             , PASSWORD				AS PASSWORD
             , PASSWORD_HINT		AS PASSWORD_HINT
             , PASSWORD_CNSR		AS PASSWORD_CNSR
             , IHIDNUM				AS IHIDNUM
             , SEXDSTN_CODE			AS SEXDSTN_CODE
             , ZIP					AS ZIP
             , HOUSE_ADRES			AS ADRES
             , AREA_NO				AS AREA_NO
             , EMPLYR_STTUS_CODE	AS MBER_STTUS
             , DETAIL_ADRES			AS DETAIL_ADRES
             , HOUSE_END_TELNO		AS END_TEL_NO
             , MBTLNUM				AS MOBL_PHON_NO
             , GROUP_ID				AS GROUP_ID
             , FXNUM				AS MBER_FXNUM
             , EMAIL_ADRES			AS MBER_EMAIL_ADRES
             , HOUSE_MIDDLE_TELNO	AS MIDDLE_TEL_NO
             , SBSCRB_DE			AS SBSCR_DE
          FROM LETTNEMPLYRINFO
         WHERE ESNTL_ID = #{uniqId}
    </select>
    
    <update id="updateMber_S">
        UPDATE LETTNEMPLYRINFO 
           SET EMPLYR_ID			= #{mberId}
             , USER_NM				= #{mberNm}
             , PASSWORD_HINT		= #{passwordHint}
             , PASSWORD_CNSR		= #{passwordCnsr}
             , IHIDNUM				= #{ihidnum}
             , SEXDSTN_CODE			= #{sexdstnCode}
             , ZIP					= #{zip}
             , HOUSE_ADRES			= #{adres}
             , AREA_NO				= #{areaNo}
             , EMPLYR_STTUS_CODE	= #{mberSttus}
             , DETAIL_ADRES			= #{detailAdres}
             , HOUSE_END_TELNO		= #{endTelno}
             , MBTLNUM				= #{moblphonNo}
             , GROUP_ID				= #{groupId}
             , FXNUM				= #{mberFxnum}
             , EMAIL_ADRES			= #{mberEmailAdres}
             , HOUSE_MIDDLE_TELNO	= #{middleTelno}
         WHERE ESNTL_ID = #{uniqId}
    </update>
    
    <select id="selectStplat_S" resultMap="stplatMap">
        <!-- 
        SELECT
            USE_STPLAT_ID           ,
            USE_STPLAT_CN           ,
            INFO_PROVD_AGRE_CN      
        FROM LETTNSTPLATINFO
        WHERE USE_STPLAT_ID=#{stplatId}
         -->
        
        SELECT 
        	   '' AS USE_STPLAT_ID
             , '' AS USE_STPLAT_CN
             , '' AS INFO_PROVD_AGRE_CN 
          FROM DUAL
         WHERE 1 = 1
    </select>
    
    <update id="updatePassword_S">
        UPDATE LETTNEMPLYRINFO 
           SET PASSWORD	= #{password}
         WHERE ESNTL_ID	= #{uniqId}
    </update>
    
    <select id="selectPassword_S" resultType="mberVO">
        SELECT
                PASSWORD		AS PASSWORD 
          FROM  LETTNEMPLYRINFO
         WHERE  ESNTL_ID = #{uniqId}
    </select>
    
    <select id="checkIdDplct_S" resultType="int">
         SELECT COUNT(1)	AS USED_CNT
          FROM (
	            SELECT
	                   EMPLYR_ID			AS USER_ID
	              FROM LETTNEMPLYRINFO
	            UNION ALL
	            SELECT
	                   ENTRPRS_MBER_ID		AS USER_ID
	              FROM LETTNENTRPRSMBER
	            UNION ALL
	            SELECT
	                   MBER_ID				AS USER_ID
	              FROM LETTNGNRLMBER
	            ) A
         WHERE USER_ID = #{checkId}
    </select>
</mapper>