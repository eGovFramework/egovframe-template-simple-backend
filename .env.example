# docker-compose 실행 시 사용하는 환경 변수.
# 본 파일을 ".env"로 복사한 뒤 실제 값을 채워서 사용합니다.

APP_VERSION=5.0.0
MYSQL_VERSION=8.0.39

# 데이터베이스 자격증명 — 운영 환경에서는 충분히 강한 무작위 문자열로 교체
MYSQL_DATABASE=egovdb
MYSQL_USER=egov
MYSQL_PASSWORD=changeme
MYSQL_ROOT_PASSWORD=changemeRoot
